#!/usr/bin/env bash
set -euo pipefail
MSG="${1:-update: $(date -Iseconds)}"
sudo nix flake update --flake /etc/nixos
sudo nixos-rebuild switch --flake /etc/nixos#nixos
"$HOME/dotfiles/pushconfigs.sh" "$MSG"
